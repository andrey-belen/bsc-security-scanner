# BSC Security Scanner Configuration

# Server
PORT=3001
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Etherscan API Key (works for BSCScan - get from https://etherscan.io/myapikey)
# BSCScan now uses Etherscan infrastructure, so use an Etherscan API key
ETHERSCAN_API_KEY=your_etherscan_api_key_here

# Legacy support (optional, will use ETHERSCAN_API_KEY if not set)
BSCSCAN_API_KEY=your_etherscan_api_key_here

# Rate Limiting
RATE_LIMIT_MAX=10
CACHE_TTL=300